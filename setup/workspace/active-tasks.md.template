# Active Tasks · 任务进度持久化

> 这是 LLM 跨会话记忆的关键文件。
> 每次启动 skill 前必读，每次完成任务后必更新。
> 治"健忘"的核心机制——确保任何任务被打断后，下次能从断点继续。

---

## 进行中的任务

### task-001

- **id**: task-001
- **created**: 2026-04-07
- **target**: 寒武纪 (688256.SH)
- **skill**: ZZG
- **type**: deepdive
- **status**: in_progress
- **progress**: 已完成 §1 公司定位 / §2 业务拆分 / §3 收入驱动；卡在 §4 利润驱动，需补 2024 Q4 毛利率
- **blocker**: 缺 2024 Q4 实际毛利率（iFind 暂未披露）
- **next_step**: 等 Q4 财报发布或先用 Q3 做内部推演，正文注明尚未披露即可
- **expected_output**: coverage/688256_寒武纪/deepdive/2026-04-07-deepdive.md
- **last_touched**: 2026-04-07 14:30

### task-002

- **id**: task-002
- **created**: 2026-04-06
- **target**: 中芯国际 (688981.SH / 0981.HK)
- **skill**: sm-earnings-preview
- **type**: earnings_preview
- **status**: in_progress
- **progress**: 已完成市场关注三个问题 + 敏感变量表，需补管理层指引听点
- **blocker**: 无
- **next_step**: 拉最近 2 次业绩会 transcript 完成指引听点段
- **expected_output**: coverage/688981_中芯国际/earnings/2026-Q4-preview.md
- **last_touched**: 2026-04-06 22:15

---

## 已完成的任务（保留 30 天，之后归档）

### task-099

- **id**: task-099
- **target**: 宁德时代 (300750.SZ)
- **skill**: sm-thesis
- **status**: done
- **completed_at**: 2026-04-05 16:20
- **output**: coverage/300750_宁德时代/thesis/2026-04-05-thesis.md
- **outcome_summary**: 命题"产能出清 + 储能放量"，3 个必要条件已列，跟踪 Q1 业绩会

---

## 待开始的任务（队列）

### queue-001

- **target**: 海光信息 (688041.SH)
- **skill**: ZZG
- **type**: new_coverage
- **priority**: high
- **created**: 2026-04-07
- **trigger**: PM 询问 AI 算力国产替代第二选择
- **deadline**: 2026-04-12

### queue-002

- **target**: AI 算力 capex 主题
- **skill**: sm-red-team
- **type**: thesis_challenge
- **priority**: medium
- **trigger**: 当前组合 AI 算力暴露已达 25%，需要对自己的多头逻辑做反方
- **deadline**: 2026-04-10

---

## 字段说明

| 字段 | 含义 |
|---|---|
| **id** | 唯一 ID（task-NNN，简单递增） |
| **created** | 任务创建日期 |
| **target** | 标的（公司、行业、主题），含 ticker |
| **skill** | 用哪个 sm-* skill 执行 |
| **type** | 任务类型（new_coverage / update / preview / postmortem / thesis_challenge ...） |
| **status** | `in_progress` / `done` / `blocked` / `cancelled` |
| **progress** | 文字描述当前做到哪一步 |
| **blocker** | 当前卡在哪（"无"或具体说明） |
| **next_step** | 下次接着做什么 |
| **expected_output** | 预期归档路径（按 output-archive.md 规范） |
| **last_touched** | 最后一次更新时间 |
| **completed_at** | 完成时间（done 状态才有） |
| **outcome_summary** | 完成时的一句话总结（done 状态才有） |
| **priority** | 优先级（high / medium / low），仅 queue 有 |
| **trigger** | 任务触发原因（谁要求的、什么事件触发的） |
| **deadline** | 截止日期 |

---

## LLM 使用本文件的规则

### 启动 skill 前（preamble.md Step 3）

1. 读本文件
2. 找：是否存在 `target` 与本次任务相同的 in_progress 任务？
3. 如果有 → 读 `progress` + `next_step`，从断点继续
4. 如果无 → 创建一条新的 in_progress 任务，分配新 id，写入文件

### 完成 skill 后（postamble.md Step 5）

1. 找到本次对应的 task（按 id）
2. 如果完成全部任务：
   - status → done
   - completed_at → 当前时间
   - output → 实际归档路径
   - outcome_summary → 一句话总结
3. 如果只完成部分：
   - 更新 progress 字段
   - 更新 last_touched
   - 如果遇到 blocker，写入 blocker 字段
   - 写入 next_step
4. 如果产生了后续任务 → 添加到 queue 区

### 用户启动新会话时（CLAUDE.md preamble）

1. 读本文件，列出所有 in_progress 任务
2. 在会话开头主动说：
   "你有 3 个进行中的任务：寒武纪 deepdive（卡在 Q4 毛利率）、中芯国际 earnings preview（缺指引听点）、海光 deepdive（待开始）。要继续哪一个？"
3. 不要等用户问

---

## 维护原则

- in_progress 不超过 5 个（超过说明上下文太散，应该清理或合并）
- 完成的任务保留 30 天后归档到 `archive/active-tasks-{YYYY-MM}.md`
- queue 不超过 20 个（超过说明优先级管理失败）
- 每周复盘一次：检查是否有 in_progress > 7 天没动的"僵尸任务"，要么继续做要么 cancel

---

## 例外说明

**仅以下情况允许跳过 active-tasks.md 的更新**：

- 用户明确说"这次只是问个简单问题，不需要任务化"
- 一次性的事实查询（如"宁德时代 2024 营收是多少"）

**任何复杂分析任务都必须经过 active-tasks.md 的进出。**
