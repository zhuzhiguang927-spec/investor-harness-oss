---
name: 我的晨会日报
trigger:
  - "跑一下日报"
  - "跑一下今天的晨会"
  - "生成晨会简报"
  - "daily briefing"
frequency: daily
based_on_skill: sm-briefing
output_to: "briefings/{YYYY-MM-DD}-morning.md"
market: CN-A
coverage_scope: watchlist
---

# 我的晨会日报模板（示例）

> 这是 bootstrap 时自带的示例模板。你可以直接用，也可以改成自己的。

## 模板说明

每个工作日早上自动跑一次，生成给我和团队的晨会简报。
重点：不堆砌信息，只列今天真正需要关注的。

## 输入（LLM 自动获取）

- 从 `coverage.md` 读覆盖池
- 从 news MCP 拉近 12 小时资讯（优先海外隔夜 + 国内早盘公告）
- 从 `watchlist.md` 读重点关注标的
- 从 `active-tasks.md` 读未完成任务

## 输出结构（5 段，严格）

### 1. 开盘前 3 件最重要的事

只列 3 件。按重要性排序。
每件要说清：**是什么** + **对谁相关** + **该怎么反应**。

不列的：
- ❌ 宏观数据里"略超预期"的
- ❌ 行业层面已经连续 discuss 超过一周的
- ❌ 和我覆盖池无关的纯新闻

### 2. 覆盖池异动

只列涨跌幅 > 3% 或成交量异动 > 2 倍均量的标的。

每条格式：
```
{ticker} {name}  涨跌 ±X.X%  量比 X.X  原因 [一句话]
```

原因需经过后台可靠性自检；正文不做标签化来源分级。

### 3. 今日关键财报 / 会议 / 事件

按发生时间排序。格式：
```
HH:MM  {事件名}  关联标的  预期关注点
```

### 4. 待我决策的事项

从 active-tasks.md 里提取 status=blocked 或 deadline=today 的任务。
不要编造，只列真实存在的 action items。

### 5. 一句话给自己

复盘昨天的一个判断，或者提醒今天的一个偏差。
来自 `decision-log.md` 和 `biases.md`。

## 约束

- 总长度 < 500 字
- 每条关键事实必须经过后台可靠性自检
- 不用"宏观波动""地缘政治"这种套话
- 不给"必涨 / 必跌"
- 涉及具体加减仓建议必须标"需人工复核"
- 输出后更新 coverage.md 的"最后扫描时间"

## 触发方式

- 直接说"跑一下日报"
- 或者每天早上 9 点由 scheduled-tasks 自动触发
- 手动测试：`用 my daily-briefing 模板跑一下`

## 归档

每次归档到 `{workspace}/briefings/{YYYY-MM-DD}-morning.md`。
这样半年后你能回看"今天是我两个月前最关注的事"，验证自己的判断。

---

> 📝 **怎么改造这个模板**：
> 1. 改 `trigger:` 列表为你习惯说的话
> 2. 改"5 段"为你想要的段数和重点
> 3. 改 `coverage_scope:` 为 `all` / `watchlist` / 特定板块
> 4. 改"约束"里的字数限制、禁用词
> 5. 加你自己团队的合规要求
