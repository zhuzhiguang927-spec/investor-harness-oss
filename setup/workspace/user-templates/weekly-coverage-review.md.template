---
name: 每周覆盖池复盘
trigger:
  - "跑一下周度复盘"
  - "这周的覆盖池回顾"
  - "weekly review"
frequency: weekly
based_on_skill: sm-batch-refresh
output_to: "briefings/weekly/{YYYY}-W{WEEK}-coverage-review.md"
coverage_scope: all
---

# 每周覆盖池复盘模板（示例）

> 每周五（或周日晚）跑一次，让 LLM 批量扫描你所有的覆盖池标的，挑出本周真正值得注意的。

## 输入

- `coverage.md` 的全部 in-focus 标的
- 近 7 天的资讯、公告、异动
- `decision-log.md` 里本周的决策记录
- `active-tasks.md` 里本周完成 / 卡住的任务

## 输出结构（6 段）

### 1. 本周覆盖池整体表现

格式：
```
覆盖池 N 家
涨跌幅中位数：X%
跑赢基准：N 家
跑输基准：N 家
最亮眼 Top 3：
最拉垮 Bottom 3：
```

### 2. 本周发生了什么（按重要性）

只列"会影响我核心命题"的事件。不列纯噪音。
每条格式：
```
{ticker} {name}  [事件类型]  事件描述  对命题的影响
```

影响分类：✅ 支持 / ⚠️ 动摇 / ❌ 证伪 / 🔹 中性

### 3. 命题状态检查

对每个活跃命题（从 active thesis 里拿）：
```
{命题名}
  当前状态：成立 / 部分成立 / 证伪风险上升 / 证伪
  本周验证到的证据：
  本周反面证据：
  下周需要看的数据点：
```

### 4. 本周决策复盘

从 decision-log 提取本周的所有决策：
```
{日期}  {标的}  {决策}  {当时理由摘要}  {本周表现 vs 预期}
```

对"理由和结果明显不一致"的决策**重点标出**，准备下周做 sm-red-team 反方审视。

### 5. 下周重点

1. 要跑 sm-company-deepdive 更新的标的（上次 deepdive > 3 个月前的）
2. 即将出财报的标的（下周内）
3. 卡在 blocked 的任务（需要数据 / 需要决策）
4. 新加入观察池的标的

### 6. 偏差检查

本周复盘 `biases.md`：
- 是否反复犯了某个已知偏差？
- 是否出现新的偏差模式需要加入 biases.md？

## 约束

- 总长度 800-1500 字
- 每条关键数据需有来源或口径备查
- 命题状态变化必须有硬证据支撑
- 不给"继续持有 / 减仓"等具体建议（这是每周复盘，不是投资决策）

## 触发方式

- 说"跑一下周度复盘"
- 每周五下午 17:00 由 scheduled-tasks 自动触发

## 归档

```
{workspace}/briefings/weekly/{YYYY}-W{WEEK}-coverage-review.md
```

例如：`2026-W15-coverage-review.md`

---

> 📝 这个模板是"季度末复盘"的缩减版。季度末可以再搭一个 `quarterly-review.md.template`，加入行业对比、策略复盘、组合归因等。
