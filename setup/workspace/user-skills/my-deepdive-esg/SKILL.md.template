---
name: my-deepdive-esg
extends: sm-company-deepdive
description: 在标准公司深度 9 段基础上，新增 §8.5 ESG 专项段。适合有 ESG 策略或需要合规对标的机构用户。
inputs:
  - 公司名 / 代码
  - 可选：MSCI ESG 评级 / 中证 ESG / 公司 ESG 报告
outputs:
  - 继承 sm-company-deepdive 的 9 段结构
  - 新增 §8.5 ESG 专项段
data_sources:
  - 见 ../../../core/adapters.md
  - MSCI ESG / Sustainalytics / 中证 ESG（需用户自配）
markets: [CN-A, HK, US]
---

# 我的公司深度（含 ESG 专项）· 示例 L2 扩展 skill

> 这是一个 L2 继承 skill 的示例。
> 它继承 `sm-company-deepdive` 的所有内容，只在 §8 和 §9 之间新增一个 §8.5 ESG 专项段。

## 继承声明

**继承的**（不动）：
- `core/preamble.md` 的 6 步开始前流程
- `core/postamble.md` 的 8 步结束后流程
- sm-company-deepdive 的 9 段输出结构
- 来源/口径纪律
- 合规边界
- 归档协议

**新增的**（本 skill 的唯一改动）：
- 在 §8（未来三个月跟踪指标）和 §9（仍需补的资料）之间，插入 §8.5

## §8.5 ESG 专项（新增段）

### 环境 (E)
- 碳排放强度：tCO₂e / 百万元营收（公司披露-公司披露 或 自行推演-推算）
- 可再生能源占比：X%（注明来源或口径；无法确认则只作背景）
- 水资源管理：取水 / 排放 / 循环
- 废物处理：危废 / 一般废物 / 循环经济指标

### 社会 (S)
- 员工流失率（行业对比） (公司披露-社会责任报告)
- 安全事件数 vs 行业均值 (公司披露)
- 供应链审计：劳工 / 人权 / 反腐 (公司披露 或 市场共识)
- 社区影响：纳税 / 就业 / 产业带动

### 治理 (G)
- 董事会独立性：独董占比 (公司披露)
- 高管薪酬与业绩挂钩比例 (公司披露)
- 审计事务所（大小所）(公司披露)
- 关联交易规模与比例 (公司披露)
- 股东诉讼历史 (已核验事实)

### ESG 对核心投资命题的影响 ⭐

这是 §8.5 最关键的一段。不能只列数据，必须说：

1. **ESG 因素是否支持核心命题？**
   （例：公司宣称"碳中和龙头"，但实际碳排强度高于行业均值 → 命题受到质疑）

2. **哪些 ESG 风险可能颠覆命题？**
   （例：美国 SEC 气候披露新规 2026 生效，公司若达不到会被海外机构撤资）

3. **未来 ESG 监管趋严场景下的加减分？**
   （例：欧盟 CBAM 生效 → 高耗能出口型公司成本结构恶化）

4. **ESG 相关的催化剂 / 风险事件**（3 个月内）
   （例：即将发布 ESG 报告 / 即将被 MSCI 重新评级 / 相关诉讼判决）

## 约束（在继承的基础上叠加）

- ESG 数据来源必须明确：
  - MSCI / Sustainalytics → 标 `公司披露`（权威第三方）
  - 公司自披露 → 标 `自行推演`（需验证）
  - 新闻推测 → 仅作背景，不能作为结论核心依据
- **禁止漂绿（greenwashing）**：公司 ESG 宣传与数据不符时必须显式指出
- §8.5 长度控制在 600-800 字，避免稀释核心分析
- 不给 ESG 评分（那是评级机构的事）
- 涉及"ESG 改善的投资机会"必须带"需人工复核"标注

## 触发方式

- "用我的带 ESG 的深度看一下 X"
- "X 的 ESG 深度"
- "用 my-deepdive-esg 看 X"
- "X 的深度（含 ESG）"

## 归档路径

继承 sm-company-deepdive 的归档路径：

```
{coverage_root}/{ticker}_{name}/deepdive/{YYYY-MM-DD}-deepdive-esg.md
```

注意文件名用 `deepdive-esg` 后缀，和标准 deepdive 区分开。

## 执行逻辑（给 LLM 的指令）

1. 读本文件确认是 L2 extends skill
2. 加载父 skill `sm-company-deepdive/SKILL.md`
3. 按父 skill 的完整流程执行
4. 在生成 §8 后、§9 前，插入 §8.5
5. §9 保持原样
6. 归档时文件名加 `-esg` 后缀
7. 所有其他纪律（来源/口径自检 / 合规 / Dual Output）**完全继承**

---

> 📝 **想把这个改成你自己的扩展**？
> 1. 复制整个目录：`cp -r my-deepdive-esg my-deepdive-custom`
> 2. 改 frontmatter `name` 和 `description`
> 3. 把 §8.5 改成你想新增的段（可以是"行业对比专项"、"渠道验证"、"产业链上下游访谈"等）
> 4. 不要改 `extends:` 字段（除非你要继承其他 sm-*）
