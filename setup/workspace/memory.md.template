# 投研记忆索引

> LLM 在每次会话开始时读这个文件，决定加载什么上下文。
> 不要把全部历史塞进来——只放"索引"和"路标"。

---

## 我的研究身份

- **角色**：[请填：卖方分析师 / 买方研究员 / 基金经理 / 个人投资者 / 学习中]
- **覆盖范围**：[请填：例 A 股科技 / 美股半导体 / 港股新消费]
- **时间窗口偏好**：[请填：3-12 个月 / 长期价值 / 事件驱动]
- **风险承受度**：[请填：低 / 中 / 高]

---

## 触发约定

LLM 看到以下关键词时按对应方式处理：

- **公司名 / ticker** → 先查 `coverage.md` 是否在覆盖池；正式公司研究 / 个股分析默认走 `company-analysis`，命题拆解才走 `sm-thesis`
- **行业 / 主题词** → 走 `sm-industry-map` + `sm-thesis`
- **财报 / 业绩** → 走 `sm-earnings-preview`
- **政策 / 发布会 / 价格** → 走 `sm-catalyst-monitor`
- **"反过来想 / 空头视角"** → 走 `sm-red-team`
- **"给 PM"** → 走 `sm-pm-brief`
- **"晨会 / 晚报 / 整理"** → 走 `sm-briefing`
- **"模型 / 估值 / DCF"** → 走 `sm-model-check`
- **"路演 / 调研 / 业绩会"** → 走 `sm-roadshow-questions`
- **"一致预期 / 预期差"** → 走 `sm-consensus-watch`

---

## 当前活跃的投资命题

每条命题对应 `theses/{name}.md`（你可以按需在工作区创建 `theses/` 子目录）。

- [ ] 命题 1：[请填，例 "AI 算力 capex 进入第二轮加速"]
  - 主仓位：[相关公司]
  - 关键验证节点：[请填日期或事件]
- [ ] 命题 2：...
- [ ] 命题 3：...

---

## 覆盖池快速引用

详见 [coverage.md](coverage.md)。当前覆盖 N 家公司：

- [覆盖池公司 1]
- [覆盖池公司 2]
- ...

LLM 看到这些公司名时，默认认为是"我的覆盖标的"，按 deepdive 模式给出更细致的输出。

---

## 已知偏差

详见 [biases.md](biases.md)。每次给决策结论前 LLM 必须检查。

---

## 数据源

- [ ] 妙想 skill（A 股 / 公募 / 宏观 / 资讯）— 是否已配置：
- [ ] cn-web-search skill — 是否已装：
- [ ] WebSearch / WebFetch（harness 内置）— 默认可用
- [ ] 其他专业终端（Wind / Bloomberg / FactSet）— 是否有访问：

---

## 不交叉的边界

- 投研任务和其他生活任务不交叉混答。如果我问"今天吃什么"，不要往投资上扯。
- 个人持仓信息不出现在 LLM 的输出里，除非我主动问"我自己的仓位"。
